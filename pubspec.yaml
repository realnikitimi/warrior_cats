name: warrior_cats
description: "A new Flutter project."
publish_to: "none"
version: 0.1.1

environment:
  sdk: ^3.8.1

dependencies:
  file_picker: ^10.2.1
  flutter:
    sdk: flutter
  http: ^1.4.0
  http_parser: ^4.1.2
  permission_handler: ^12.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
